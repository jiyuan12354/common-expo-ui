import { AppRoutes, AppNavigatorParams } from 'common_services_frontend';

export type OTPScreenParams = {
  agentCode: string;
  phoneNum: string;
  username: string;
  enableVerificationTrials?: boolean; //For EPA services OTP, Support the 3 & 6 times fail attempt mechanism imposed by IT Security
  successCallback: () => void;
};

export type PDFViewerParams = {
  pdfName: string;
  pdfUrl: string;
  pdfPassword?: string;
}

export type CommonNavigatorParams = AppNavigatorParams & {
  [AppRoutes.OTP_SCREEN]: OTPScreenParams;
  [AppRoutes.PDF_VIEWER]: PDFViewerParams;
};
