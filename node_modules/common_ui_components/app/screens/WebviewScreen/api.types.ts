import { GeneralApiProblem } from 'common_services_frontend';

export interface SaamResponse {
  issuer: string;
  userRole: string;
  cookieKey: string;
  cookieValue: string;
  statusCode: number;
}

export type GetSAAMTokenResult = { kind: 'ok'; saamResponse: SaamResponse } | GeneralApiProblem;
