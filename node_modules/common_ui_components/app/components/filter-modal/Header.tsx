import React, { useEffect, useCallback, useRef } from 'react';
import { View, Text, useWindowDimensions, TouchableOpacity, Animated } from 'react-native';
import {
  CommonTranslateKey,
  PruHeader,
  PruPalette,
  PruScreen,
  PruColor,
  PruActivityIndicator,
  textStyleSet,
  PruIconFont
} from 'common_ui_components';
import { constants, pruTestID } from 'common_services_frontend';
import { useTranslation } from 'react-i18next';
import { useDispatch, useSelector } from 'react-redux';


import { styles } from './style';


const Header: React.Fc<any> = ({
  modalTitle,
  dismissCallBack
}) => {
  const { t, i18n } = useTranslation();
  const dispatch = useDispatch();


  return (
    <View style={styles.header}>
      <Text
        style={styles.headerTitle}>
        {modalTitle || t(CommonTranslateKey.filter)}
      </Text>
      <TouchableOpacity
        accessible={false}
        activeOpacity={0.9}
        onPress={() => {
          dismissCallBack();
        }}
        style={{ position: 'absolute', right: 0, top: 16 }}>
        <PruIconFont
          {...pruTestID('closeIcon')}
          name="close"
          color={PruPalette.grey85}
          size={24}
        />
      </TouchableOpacity>
    </View>
  );
};


export default Header;