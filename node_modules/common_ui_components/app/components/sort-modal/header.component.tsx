import React from 'react';
import { View, Text, TouchableOpacity, ViewStyle } from 'react-native';
import { PruPalette, PruIconFont } from 'common_ui_components';
import { pruTestID } from 'common_services_frontend';
import { styles } from './sort-modal.style';

export const HeaderComponent: React.FC<any> = ({ modalTitle, dismissCallBack }) => {
  return (
    <View style={styles.header}>
      <Text style={styles.headerTitle}>{modalTitle}</Text>
      <TouchableOpacity
        accessible={false}
        activeOpacity={0.9}
        onPress={dismissCallBack}
        style={styles.headerTouch as ViewStyle}>
        <PruIconFont {...pruTestID('closeIcon')} name="close" color={PruPalette.grey85} size={24} />
      </TouchableOpacity>
    </View>
  );
};
