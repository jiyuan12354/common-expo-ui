import { ViewStyle } from 'react-native';
import { palette } from '../../../theme';

export const itemSeparatorStyles = {
  WRAPPER: {
    alignSelf: 'center',
    height: 1,
    backgroundColor: palette.greyeb,
  } as ViewStyle,
};
