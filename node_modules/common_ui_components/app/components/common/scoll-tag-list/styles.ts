import { ViewStyle, Platform, Dimensions } from 'react-native';
import { color as PruColor, palette, textStyleSet } from 'common_ui_components/app/theme';

export const styles = {
  mutiLineWrapper: {
    width: Dimensions.get('window').width,
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center', flexWrap: 'wrap'
  },
  item: {
    borderRadius: 19,
    borderColor: PruColor.gray,
    borderWidth: 1,
    backgroundColor: palette.white,
    marginRight: 12,
    justifyContent: 'center',
    paddingHorizontal: 12,
    paddingVertical: 3
  },
  itemSelected: {
    borderColor: palette.red,
    backgroundColor: palette.redLightest,
  },
  itemText: {
    ...textStyleSet.content_14_bold,
    color: palette.grey99
  },
  itemTextSelected: {
    color: palette.red
  }
};
