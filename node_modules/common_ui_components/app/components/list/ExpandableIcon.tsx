import React, { FC, useState} from "react";
import { View, ViewProps, StyleSheet, ViewStyle, StyleProp } from "react-native";
import Svg, {
  Path,
  Circle
} from "react-native-svg";
export enum TipCursorStyle {
	LEFT,
  RIGHT,
  CENTER,
}

type ExpandableIconProps = ViewProps & {
  isExpand?: boolean;
  leftIconColor? : string;
  leftIconStyle?: StyleProp<ViewStyle>;
}

const styles = StyleSheet.create({
  icon: {
    padding: 10,
    paddingEnd: 2
  },
  iconStyle: {
    width: 8, 
    height: 8, 
    borderWidth: 2, 
    borderRadius: 4,
  }
});

const ExpandableIcon: FC<ExpandableIconProps> = ({ 
  isExpand, 
  color = '#E8192C',
  leftIconStyle,
   ...props
 }) => {
  return (
    <View style={[styles.icon, leftIconStyle]}>
      <View style={[styles.iconStyle, { borderColor: color, backgroundColor: isExpand ? color : 'transparent' }]}/>
    </View>
    )
};

export default ExpandableIcon;
