import * as React from 'react';
import { storiesOf } from '@storybook/react-native';
import { StoryScreen, Story, UseCase } from '../../../../storybook/views';
import { Icon } from './icon';
import { palette } from '../../../theme';

declare let module: any;

storiesOf('PNG Icon', module)
  .addDecorator((fn: () => any) => <StoryScreen>{fn()}</StoryScreen>)
  // .addParameters({ notes: 'This component is currently unused in-app.' })
  .add('Icon style', () => (
    <>
      <Story>
        <UseCase text="No Content" usage="The icon for no content">
          <Icon
            icon="no_content"
            style={{
              height: 180,
              width: 150,
            }}
            containerStyle={{
              borderWidth: 1,
              borderColor: palette.cobalt,
              alignItems: 'center',
              height: 180,
            }}
          />
        </UseCase>
        <UseCase text="No Network Connected" usage="The icon for a no network connected">
          <Icon
            icon="no_network_connected"
            style={{
              height: 180,
              width: 150,
            }}
            containerStyle={{ borderWidth: 1, borderColor: palette.red }}
          />
        </UseCase>
      </Story>
    </>
  ));
