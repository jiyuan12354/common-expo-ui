import { ViewStyle, TextStyle, Dimensions } from 'react-native';
import { color, typography } from 'common_ui_components/app/theme';

export const eventListStyles = {
  WRAPPER: {
    justifyContent: 'flex-start',
    // marginBottom: 20,
    flex: 1
  } as ViewStyle,
  TEXT: {
    fontFamily: typography.primary,
    fontSize: 14,
    color: color.primary,
  } as TextStyle,
};
