import { ViewStyle, Dimensions } from 'react-native';

export const materialPageStyles = {
  WRAPPER: {
    justifyContent: 'center',
    alignItems: 'center',
    // paddingTop: Dimensions.get('window').width / 2
  } as ViewStyle,
};
