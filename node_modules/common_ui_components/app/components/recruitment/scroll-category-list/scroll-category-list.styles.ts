import { ViewStyle } from 'react-native';
import { palette as PruPalette } from '../../../theme/palette';

export const scrollTagListStyles = {
  WRAPPER: {
    backgroundColor: PruPalette.white,
  } as ViewStyle,
};
