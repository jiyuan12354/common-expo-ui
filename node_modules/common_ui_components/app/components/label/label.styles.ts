import { ViewStyle, TextStyle } from 'react-native';

export const labelStyles = {
  WRAPPER: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 2,
  } as ViewStyle,
  TEXT: {
    fontWeight: '500',
    textAlign: 'center',
  } as TextStyle,
};
