import * as React from 'react';
import { ViewStyle, TextStyle, Alert, View, Text } from 'react-native';
import { Story, UseCase } from '../../views';
import { PruUI } from 'common_ui_components';

export const TextIconButtonStory = () => (
  <Story>
    <UseCase text="Active" usage="The active buttons with icon.">
      <View style={{ flexDirection: 'row' }}>
        <PruUI.TextIconButton
          text="View More"
          preset="primary"
          icon="arrow"
          onPress={() => Alert.alert('primary pressed')}
        />
        <View style={{ width: 16 }} />
        <PruUI.TextIconButton
          text="View More"
          preset="secondary"
          icon="arrow"
          onPress={() => Alert.alert('secondary pressed')}
        />
      </View>
    </UseCase>
    <UseCase text="Inactive" usage="The disabled behaviour of the primary button with icon.">
      <View style={{ flexDirection: 'row' }}>
        <PruUI.TextIconButton
          text="View More"
          preset="primary"
          icon="arrow"
          onPress={() => Alert.alert('primary pressed')}
          disabled
        />
        <View style={{ width: 16 }} />
        <PruUI.TextIconButton
          text="View More"
          preset="secondary"
          icon="arrow"
          onPress={() => Alert.alert('secondary pressed')}
          disabled
        />
      </View>
    </UseCase>
    <UseCase text="Active" usage="icon first">
      <View style={{ flexDirection: 'row' }}>
        <PruUI.TextIconButton
          text="Like"
          preset="primary"
          icon="fav"
          iconFirst
          onPress={() => Alert.alert('primary pressed')}
        />
        <View style={{ width: 16 }} />
        <PruUI.TextIconButton
          text="Like"
          preset="secondary"
          icon="fav"
          iconFirst
          onPress={() => Alert.alert('secondary pressed')}
        />
      </View>
    </UseCase>
    <UseCase text="Inactive" usage="The disabled behaviour of the primary button with icon.">
      <View style={{ flexDirection: 'row' }}>
        <PruUI.TextIconButton
          text="Like"
          preset="primary"
          icon="fav"
          iconFirst
          onPress={() => Alert.alert('primary pressed')}
          disabled
        />
        <View style={{ width: 16 }} />
        <PruUI.TextIconButton
          text="Like"
          preset="secondary"
          icon="fav"
          iconFirst
          onPress={() => Alert.alert('secondary pressed')}
          disabled
        />
      </View>
    </UseCase>
  </Story>
);
