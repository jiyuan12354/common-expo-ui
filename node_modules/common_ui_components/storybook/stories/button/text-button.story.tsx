import * as React from 'react';
import { Alert, Text } from 'react-native';
import { Story, UseCase } from 'common_ui_components/storybook/views';
import { PruUI } from 'common_ui_components';

export const TextButtonStory = () => (
  <Story>
    <UseCase text="Large model" usage="Active && Disable">
      <Text>
        <PruUI.TextButton
          text="Text Button"
          model="large"
          onPress={() => {
            Alert.alert('large');
          }}
        />
        {'        '}
        <PruUI.TextButton
          text="Text Button"
          model="large"
          disabled
          onPress={() => {
            Alert.alert('large');
          }}
        />
      </Text>
    </UseCase>
    <UseCase text="Middle model" usage="Active && Disable">
      <Text>
        <PruUI.TextButton
          text="Text Button"
          model="middle"
          onPress={() => {
            Alert.alert('middle');
          }}
        />
        {'        '}
        <PruUI.TextButton
          text="Text Button"
          model="middle"
          disabled
          onPress={() => {
            Alert.alert('middle');
          }}
        />
      </Text>
    </UseCase>
    <UseCase text="Small model" usage="Active && Disable">
      <Text>
        <PruUI.TextButton
          text="Text Button"
          model="small"
          onPress={() => {
            Alert.alert('small');
          }}
        />
        {'        '}
        <PruUI.TextButton
          text="Text Button"
          model="small"
          disabled
          onPress={() => {
            Alert.alert('small');
          }}
        />
      </Text>
    </UseCase>
    <UseCase text="Large model: iconfist" usage="Active && Disable">
      <Text>
        <PruUI.TextButton
          text="New"
          prefixIcon="instruction_add"
          model="large"
          onPress={() => {
            Alert.alert('large');
          }}
        />
        {'        '}
        <PruUI.TextButton
          text="New"
          prefixIcon="instruction_add"
          model="large"
          disabled
          onPress={() => {
            Alert.alert('large');
          }}
        />
      </Text>
    </UseCase>
    <UseCase text="Middle model" usage="Active && Disable">
      <Text>
        <PruUI.TextButton
          text="New"
          prefixIcon="instruction_add"
          model="middle"
          onPress={() => {
            Alert.alert('middle');
          }}
        />
        {'        '}
        <PruUI.TextButton
          text="New"
          prefixIcon="instruction_add"
          model="middle"
          disabled
          onPress={() => {
            Alert.alert('middle');
          }}
        />
      </Text>
    </UseCase>
    <UseCase text="Small model" usage="Active && Disable">
      <Text>
        <PruUI.TextButton
          text="New"
          prefixIcon="instruction_add"
          model="small"
          onPress={() => {
            Alert.alert('small');
          }}
        />
        {'        '}
        <PruUI.TextButton
          text="New"
          prefixIcon="instruction_add"
          model="small"
          disabled
          onPress={() => {
            Alert.alert('small');
          }}
        />
      </Text>
    </UseCase>
  </Story>
);
