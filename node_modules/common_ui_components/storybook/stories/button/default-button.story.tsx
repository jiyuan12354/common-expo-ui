import * as React from 'react';
import { ViewStyle, TextStyle, Alert, View, Text } from 'react-native';
import { Story, UseCase } from 'common_ui_components/storybook/views';
import { PruUI } from 'common_ui_components';
import { color } from 'common_ui_components/app/theme';

export const DefaultButtonStory = () => (
  <Story>
    <UseCase text="Normal" usage="Width Adaptive">
      <Text>
        <View
          style={{
            flexDirection: 'row',
          }}>
          <PruUI.Button text="buttonA" preset="primary" onPress={() => Alert.alert('a pressed')} />
          <View style={{ width: 16 }} />
          <PruUI.Button
            text="buttonB"
            preset="secondary"
            onPress={() => Alert.alert('b pressed')}
          />
        </View>
      </Text>
    </UseCase>
    <UseCase
      text="Active"
      usage="use matchParent to stretch button to parent: default model is stretch">
      <View
        style={{
          backgroundColor: color.yellowLight,
        }}>
        <PruUI.Button
          text="Stretch to container"
          preset="primary"
          onPress={() => Alert.alert('primary pressed')}
        />
        <View style={{ height: 16 }} />
        <PruUI.Button
          text="Stretch to container"
          preset="secondary"
          onPress={() => Alert.alert('secondary pressed')}
        />
      </View>
      <View
        style={{
          marginTop: 16,
          backgroundColor: color.blueLight,
        }}>
        <PruUI.Button
          matchParent={false}
          text="Centralized to container"
          preset="primary"
          onPress={() => Alert.alert('primary pressed')}
        />
        <View style={{ height: 16 }} />
        <PruUI.Button
          matchParent={false}
          text="Centralized to container"
          preset="secondary"
          onPress={() => Alert.alert('secondary pressed')}
        />
      </View>
    </UseCase>
    <UseCase text="Active" usage="The flexiable buttons.">
      <View style={{ flexDirection: 'row' }}>
        <PruUI.Button
          style={{ flex: 1 }}
          text="Cancel"
          preset="secondary"
          onPress={() => Alert.alert('I am flexiable')}
        />
        <View style={{ width: 16 }} />
        <PruUI.Button
          style={{ flex: 1 }}
          text="Confirm"
          preset="primary"
          onPress={() => Alert.alert('I am flexiable')}
        />
      </View>
    </UseCase>
    <UseCase text="Inactive" usage="The disabled behaviour of the primary button.">
      <PruUI.Button
        text="Primary Disabled Button"
        preset="primary"
        onPress={() => Alert.alert('primary pressed')}
        disabled
      />
      <View style={{ height: 16 }} />
      <PruUI.Button
        text="Secondary Disabled Button"
        preset="secondary"
        onPress={() => Alert.alert('secondary pressed')}
        disabled
      />
    </UseCase>
    <UseCase text="Customized Style" usage="Button with array style">
      <View style={{ flexDirection: 'row' }}>
        <PruUI.Button
          text="Customized Button"
          preset="primary"
          onPress={() => Alert.alert('pressed')}
          style={[{ paddingVertical: 100 }, { borderRadius: 0 }]}
          textStyle={[{ fontSize: 20 }, { color: color.redLightest }]}
        />
        <View style={{ width: 16 }} />
        <View>
          <PruUI.Button
            text="Small Button"
            preset="primary"
            onPress={() => Alert.alert('pressed')}
            style={[{ paddingVertical: 0 }, { borderRadius: '50%' }]}
            textStyle={[{ fontSize: 12 }, { color: color.redLightest }]}
          />
          <View style={{ height: 16 }} />
          <PruUI.Button
            text="Small Button"
            preset="secondary"
            onPress={() => Alert.alert('pressed')}
            style={[{ paddingVertical: 0 }, { borderRadius: '50%' }]}
            textStyle={[{ fontSize: 12 }, { color: color.redLight }]}
          />
        </View>
      </View>
    </UseCase>
  </Story>
);
