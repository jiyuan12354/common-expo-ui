import * as React from 'react';
import { ViewStyle, TextStyle, Alert, View, Text } from 'react-native';
import { Story, UseCase } from '../../views';
import { PruUI } from 'common_ui_components';
import { textStyleSet } from 'common_ui_components/app/theme';

export const RoundBaseButtonStory = () => (
  <Story>
    <UseCase text="Primary" usage="Normal, disable">
      <View style={{ flexDirection: 'row', flexWrap: 'wrap' }}>
        <View
          style={{
            alignItems: 'center',
            paddingHorizontal: 8,
            marginVertical: 8,
          }}>
          <PruUI.RoundBaseButton icon="send" size={96} onPress={() => Alert.alert(`pressed`)} />
          <Text style={{ ...textStyleSet.text_16_med, textAlign: 'center' }}>{'可點擊狀態'}</Text>
        </View>
        <View
          style={{
            alignItems: 'center',
            paddingHorizontal: 8,
            marginVertical: 8,
          }}>
          <PruUI.RoundBaseButton
            size={96}
            icon="send"
            disabled
            onPress={() => Alert.alert(`pressed`)}
          />
          <Text style={{ ...textStyleSet.text_16_med, textAlign: 'center' }}>{'禁用狀態'}</Text>
        </View>
      </View>
    </UseCase>
    <UseCase text="Secondary" usage="Normal, disable">
      <View style={{ flexDirection: 'row', flexWrap: 'wrap' }}>
        <View
          style={{
            alignItems: 'center',
            paddingHorizontal: 8,
            marginVertical: 8,
          }}>
          <PruUI.RoundBaseButton
            preset="secondary"
            icon="send"
            size={96}
            onPress={() => Alert.alert(`pressed`)}
          />
          <Text style={{ ...textStyleSet.text_16_med, textAlign: 'center' }}>{'可點擊狀態'}</Text>
        </View>
        <View
          style={{
            alignItems: 'center',
            paddingHorizontal: 8,
            marginVertical: 8,
          }}>
          <PruUI.RoundBaseButton
            preset="secondary"
            size={96}
            icon="send"
            disabled
            onPress={() => Alert.alert(`pressed`)}
          />
          <Text style={{ ...textStyleSet.text_16_med, textAlign: 'center' }}>{'禁用狀態'}</Text>
        </View>
      </View>
    </UseCase>
  </Story>
);
