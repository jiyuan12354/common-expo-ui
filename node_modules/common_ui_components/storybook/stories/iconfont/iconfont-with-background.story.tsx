import * as React from 'react';
import { View, Text, ViewStyle } from 'react-native';
import { Story, UseCase } from '../../views';
import { PruUI } from 'common_ui_components';
import { icons as iconsMap, IconTypes } from '../../../app/components/icon-font/fonts/__icontype';
import { color, textStyleSet } from 'common_ui_components/app/theme';
import { IconFontLayerProps } from 'common_ui_components/app/components-ui/icon/icon-font-with-background';

const icons: Partial<Record<IconTypes, Omit<IconFontLayerProps, 'iconName'> & { desc: string }>> = {
  radio: {
    size: 96,
    iconColor: color.red,
    backgroundColor: color.redLightest,
    desc: '不帶圓角',
  },
  add: {
    size: 96,
    iconColor: color.red,
    borderRadius: 12,
    backgroundColor: color.redLightest,
    desc: '帶圓角',
  },
  scan: {
    size: 96,
    iconColor: color.red,
    borderRadius: '50%',
    backgroundColor: color.redLightest,
    desc: '圓形',
  },
  notification: {
    size: 96,
    iconColor: color.red,
    borderRadius: 12,
    backgroundColor: color.redLightest,
    borderWidth: 2,
    borderColor: color.cobalt,
    desc: '帶邊框',
  },
  train_popup: {
    size: 96,
    iconColor: color.blue,
    borderRadius: '50%',
    backgroundColor: color.blueLight,
    desc: '替換顏色',
  },
  system_popup: {
    size: 96,
    // iconSize: 24,
    iconColor: color.blue,
    borderRadius: '50%',
    backgroundColor: color.blueLight,
    backgroundOpacity: 0.5,
    desc: '調整透明度',
  },
  tabbar_me_normal: {
    size: 96,
    iconSize: 24,
    iconColor: color.blue,
    borderRadius: 12,
    backgroundColor: color.blueLight,
    desc: '調整圖標大小',
  },
};

export const IconFontLayerStory = () => (
  <Story>
    <UseCase text="Icon List" usage="">
      <Text>
        {Object.keys(icons).map((icon) => {
          return (
            <View
              key={icon}
              style={{
                alignItems: 'center',
                paddingHorizontal: 8,
                marginVertical: 8,
              }}>
              <PruUI.IconFontLayer
                backgroundColor={icons[icon].backgroundColor}
                backgroundOpacity={icons[icon].backgroundOpacity}
                borderRadius={icons[icon].borderRadius}
                borderWidth={icons[icon].borderWidth}
                borderColor={icons[icon].borderColor}
                size={icons[icon].size}
                iconName={icon as IconTypes}
                iconSize={icons[icon].iconSize}
                iconColor={icons[icon].iconColor}
              />
              <Text style={{ ...textStyleSet.text_16_med, textAlign: 'center' }}>
                {icons[icon].desc ?? icon}
              </Text>
            </View>
          );
        })}
      </Text>
    </UseCase>
  </Story>
);
