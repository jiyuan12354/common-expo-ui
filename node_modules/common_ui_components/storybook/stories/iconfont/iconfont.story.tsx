import * as React from 'react';
import { View, Text } from 'react-native';
import { Story, UseCase } from '../../views';
import { PruUI } from 'common_ui_components';
import { icons, IconTypes } from 'common_ui_components/app/components/icon-font/fonts/__icontype';
import { color } from 'common_ui_components/app/theme';

export const IconFontStory = () => (
  <Story>
    <UseCase text="Icon List" usage="">
      <Text>
        {Object.keys(icons).map((icon) => {
          return (
            <View
              key={icon}
              style={{
                alignItems: 'center',
                width: 120,
                height: 66,
                paddingHorizontal: 8,
                marginVertical: 8,
              }}>
              <PruUI.IconFont name={icon as IconTypes} size={20} color={color.redLight} />
              <Text style={{ textAlign: 'center' }}>{icon}</Text>
            </View>
          );
        })}
      </Text>
    </UseCase>
  </Story>
);
